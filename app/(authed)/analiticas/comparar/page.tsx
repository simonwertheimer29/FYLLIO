// app/(authed)/analiticas/comparar/page.tsx
// La comparación contra uno mismo: antes y después de un cambio, por clínica (MEJORAS 181).

import { CompararView } from "./CompararView";

export const dynamic = "force-dynamic";

export default function CompararPage() {
  return <CompararView />;
}
